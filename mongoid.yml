test:
  sessions:
    default:
      database: foy_test
      hosts:
        - localhost:27017
      options:
        safe: true
        consistency: :strong
        max_retries: 1
        retry_interval: 0
development:
  sessions:
    default:
      database: foy_dev
      hosts:
        - localhost:27017
      options:
        safe: true
